
const int kNumPrograms = 3;

const int kParamGain = 0;
const int kNumParams = 1;

//tags in interface builder default to 0, so avoid using that control tag
const int kCtrlTagVolumeSlider = 1;
const int kCtrlTagButton = 2;

const int kMsgTagHello = 0;
const int kMsgTagData = 1;
const int kMsgTagRestorePreset = 2;

const uint32_t kDataPacketSize = 1024; //floats
